checks:
    php:
        code_rating: true
        duplication: true
        no_exit:     false
filter:
    excluded_paths:
        - tests/*
        - bin/*
        - languages/*
tools:
    php_code_sniffer:
        config:
            standard: "WordPress"
